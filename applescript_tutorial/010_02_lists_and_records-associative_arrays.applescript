-- Using lists and records

-- records (hash in ruby or an associative array)

set myAlien to {name:"Largon", type:"Gruub", language:"Borbulian", sex:"Only with Gruubs"}

-- extract a value from the record
set thisName to name of myAlien

-- change a value in the array
set sex of myAlien to "No Thankyou"

-- perform operation on the record
set myLength to count of myAlien

