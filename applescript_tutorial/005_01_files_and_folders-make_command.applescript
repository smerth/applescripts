-- files and folders

-- under standard suite of the finder dictionary you can find "make"
-- create a new folder with the command (verb) make

tell application "Finder"
	make new folder
end tell

tell application "Finder"
	make new file
end tell