-- Functions
-- functions start with the keyword "on" followed by the function name followed by parens

on addNums(a, b)
	return a + b
end addNums

-- of course after defining the function above, you must call it

addNums(2, 3)